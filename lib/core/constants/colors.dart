import 'package:flutter/material.dart';

class AppColors{
  // Color Background And Button
    // Primary background color
    static const Color background=Color(0xFFE7E9EE);
    // Background color of button widgets
    static const Color buttonwidgetbackground=Color.fromARGB(255, 0, 0, 0);
    
    static const Color iconcolor=Color.fromARGB(255, 0, 183, 255);
    static const Color iconbackground=Color.fromARGB(255, 161, 216, 238);


    // Color Font
    // Primary font color
    static const mainfont=Color(0xFF272727);

    // Sub font color
    static const subfont=Color(0xFFFFFFFF);

    // Hint font color
    static const hintfont=Color.fromARGB(255, 0, 0, 0);

    static const color1=Color(0xFFfffbfa);

    static const color2=Color(0xFF00203E);

    static const color3=Color(0xFF787878);

}